galaxy_info:
  author: digimokan
  description: "Configure bash for interactive use"
  license: "MIT"
  min_ansible_version: 2.5
  platforms:
    - name: Archlinux
      versions:
        - all
  galaxy_tags:
    - archlinux
    - shell
    - bash

allow_duplicates: false

dependencies:
  - src: https://github.com/digimokan/ans-role-config-shell
    version: master
    tags:
      - never
  - role: ans-role-config-shell

